# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
/build
next-env.d.ts

# env — NEVER commit real keys
.env
.env*.local
.env.local

# netlify
.netlify

# misc
.DS_Store
*.pem
npm-debug.log*
.vscode/*
!.vscode/extensions.json
/coverage
*.tsbuildinfo

# Sales contact details. This repository is PUBLIC. Business phone numbers and
# websites for the outreach list are kept out of git entirely — the committed
# prospect CSV records only whether we hold one, and the values live here,
# locally, or in the private handoff.
data/revenue-readiness/local/

# Python bytecode from the data build scripts
__pycache__/

# CAT Scale proprietary source — never commit the raw CSV or full row-level detail
*extraction*.csv
data/imports/cat-scale-2026-07-29/local/

# Local email previews (scripts/preview-road-report.ts)
.road-report-preview/
