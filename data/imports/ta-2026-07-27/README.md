# TA / Petro / TA Express package — 2026-07-27

**EXECUTED 2026-07-27** under Shawn's conditional authorization: the 10-row
canary and the address-anchored blank-only enrichments were applied — **37 of
38 rows written, site 0269 TA Knoxville West quarantined** by the
coordinate-collision guard. Full audit trail in `EXECUTION-RECORD.md`.
The corrections, HOLD rows and PR merge remain separate, ungiven
authorizations. This package **contains no inserts** — every official site
already has a production row.

Generated by `scripts/generate-ta-package.mjs` from the reconciliation in
`data/sources/ta-master/2026-07-27/`, produced by `scripts/reconcile-ta.mjs`.

## Source of record

The current official TA/Petro location master, downloaded by Shawn on
2026-07-27 via **"Download Location Data"** at `ta-petro.com/location/` —
SHA-256 `a0c612f0426d141c481f84aae59dc15a0204617183bbf9c0866bfbb726ed63f7`,
**committed** as `data/sources/ta-master/2026-07-27/locmaster20260727.xlsx`
and verified: the sha matches exactly, all ten independently stated facts
reproduce from it — 354 / 97 cols / 44 states / 192+79+77+4+2 brands /
354 coords / 347 positive / 7 zero / 54,589 spaces / 0 dup Site IDs /
0 dup Location IDs — and its cell-content diff against the earlier working
copy (`5ebe0e9f…3303`) shows only two service-hours cells (TA Kingman AZ)
that no statement reads or writes. The execution precondition was satisfied
before any SQL ran.

## Reconciliation, both directions, complete

**Official side — 354 sites:**

| Bucket | Sites |
|---|--:|
| exact-match | 306 |
| safe-enrichment | 40 |
| held (Goasis 4 + Thorntons 2) | 6 |
| zero-parking (site 0347, TA Truck Service Franklin KY) | 1 |
| conflict (site 0393 — cross-brand mislabel) | 1 |
| missing / net-new | **0** |

**Production side — 395 TA-network rows:**

| Bucket | Rows |
|---|--:|
| exact-match (304 imported + 3 legacy) | 307 |
| service-only (CAT scales, truck service, roadside) | 46 |
| safe-enrichment targets | 40 |
| duplicate | 2 |
| probable closure | **0** |

The 304 imported rows are **proven** unchanged: their name+state digest
(`e7843f74…`) and full value digest (`2ac6c659…`) both equal the committed
import CSV, which derives from content-identical official data. The 91 legacy
rows are committed byte-identically (`4aabb580…`).

Matching order: **Site ID → prior-review exact production id → row-name cites
the site number → street-number/address → name tokens + city + state + zip5**,
in ordered phases so weak evidence can never steal a row that stronger
evidence explains. A loose name is never sufficient alone; ambiguity is
refused, not guessed.

## Files

| File | What | Rows |
|---|---|--:|
| `CANARY-ENRICH.sql` | 10 enrichments, 10 states — run first | 10 |
| `ENRICH-EXISTING.sql` | blank-only, exact-id, per-state (13 states) | 38 |
| `HOLD-NAME-ANCHORED.sql` | **do not run until verified** — the two Ashland VA rows | 2 |
| `CORRECTIONS-PROPOSALS.sql` | separate authorizations — see below | 2 + 1 note |
| `VERIFY.sql` / `FINGERPRINT.sql` / `ROLLBACK.sql` | read-only checks · measured baselines · value-matched reversal | — |
| `canary.json` | the canary, machine-readable | 10 |

**Invariants:** no insert, no delete, no publish, no unpublish (outside the
separately-authorized corrections), `overnight_parking` / `is_featured` /
`is_indexable` never written, coordinate-collision guard intact, Goasis and
Thorntons untouched, zero-parking locations never counted as parking.

## The corrections (separate authorizations)

**A. TA Atlanta South duplicate.** `33e41d22…` ("TA Atlanta South #268",
published) duplicates the imported row of record `15de1227…` — two published
pins ~30 m apart for official site 0268. Proposal: guarded unpublish of the
duplicate; the imported pin stays live; value-matched republish rollback
inline. Nothing deleted.

**B. The Petro Florence mislabel — two exports agree.** `beb05d53…` is named
"Love's Travel Stop #420" (Florence SC, unpublished, quarantined by the Love's
intake). Love's complete export proves #420 is Flowood **MS**; TA's master
proves site **0393 Petro Florence** sits at exactly this row's address
(3001 TV Rd, Florence SC, 210 spaces, 34.2665 / −79.7321). The row is a
mislabeled Petro. Proposal: relabel to the official identity, fill blanks from
the TA master, **keep it unpublished**. Executing B also unblocks the Love's
package's held MS #420 insert. Value-matched restore inline.

**C. TA Jacksonville South #248.** `74398e08…` duplicates the imported
`f3ec3f7f…` row but is already unpublished — quarantine record only, **no SQL**.

## The two Ashland rows (HOLD)

Ashland, Virginia has **two distinct official sites**: 0001 TA Ashland
(100 N Carter Rd, 183 spaces) and 0142 TA Richmond (10134 Lewistown Rd,
317 spaces). The prior review pointed both at the same production row. The
reconciler paired them by name — `e36e07df…` ↔ TA Ashland, `7a03c1f5…` ↔
TA Richmond — but a name pairing is below the bar for writing coordinates.
Both rows are unpublished, so nothing is wrong in front of drivers. Verify,
then run `HOLD-NAME-ANCHORED.sql`.

## Coverage — gates 4a and 4b (after the 2026-07-27 execution)

| Gate | Universe | Before | Applied | Remaining |
|---|--:|--:|--:|---|
| **4a directory** | **348** | **347** | 347 | site 0393 — needs correction B |
| **4b route-usable** | **347** | **306** sites | **343** | 0001 + 0142 (verify, publish) · 0393 (correct, publish) · **0269 (quarantined — collision guard)** |

Details and directory-wide movement in `COVERAGE.md`; the full execution
audit, including the two constraint-vocabulary aborts and the TN quarantine,
in `EXECUTION-RECORD.md`.
